{
  "name": "@workspace/infra",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "AWS CDK app for the distributed (Lambda) render backend. Lives OUTSIDE the Railway container — deployed independently to AWS, see README.md.",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "synth": "cdk synth",
    "deploy": "cdk deploy"
  },
  "dependencies": {
    "//": "Pinned EXACT to the engine line api-server runs (core/producer/player 0.6.91). renderToLambda / HyperframesRenderStack must match the deployed render runtime, so keep this in lockstep with the engine bump.",
    "@hyperframes/aws-lambda": "0.6.91"
  },
  "devDependencies": {
    "aws-cdk": "^2.166.0",
    "aws-cdk-lib": "^2.166.0",
    "constructs": "^10.4.2",
    "tsx": "catalog:",
    "typescript": "^5.9.2"
  }
}
