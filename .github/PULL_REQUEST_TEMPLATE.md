## Summary

<!-- What does this PR do, and why? Focus on the "why". -->

## Changes

-

## Related

<!-- Issue / ticket links, if any -->

## Checklist

- [ ] Conventional commit title (`feat(scope): …`, `fix: …`, `docs: …`, …)
- [ ] `pnpm run typecheck` passes
- [ ] `pnpm run lint` passes
- [ ] `pnpm test` passes
- [ ] `pnpm run build` passes
- [ ] OpenAPI codegen is in sync (run `pnpm --filter @workspace/api-spec run codegen` if the API changed)
- [ ] Tests added/updated for the change
- [ ] No generated files edited by hand; no `// @ts-ignore` / `// eslint-disable`

## Test plan

<!-- How did you verify this works? -->
