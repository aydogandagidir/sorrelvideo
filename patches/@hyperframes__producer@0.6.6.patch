diff --git a/dist/index.js b/dist/index.js
index f9c2c5c4f6876d83b3043cae436539b99a343e88..afc86c4849204350d9b5605a1b5d854b094b934e 100644
--- a/dist/index.js
+++ b/dist/index.js	
@@ -105240,6 +105240,7 @@ function buildEncoderArgs(options, inputArgs, outputPath, gpuEncoder = null) {
     } else {
       const encoderName = codec === "h264" ? "libx264" : "libx265";
       args.push("-c:v", encoderName, "-preset", preset);
+      args.push("-threads", process.env.PRODUCER_FFMPEG_THREADS || "4");
       if (bitrate) args.push("-b:v", bitrate);
       else args.push("-crf", String(quality));
       const lockGop = options.lockGopForChunkConcat === true;
@@ -105734,6 +105735,7 @@ function buildStreamingArgs(options, outputPath, gpuEncoder = null) {
     } else {
       const encoderName = codec === "h264" ? "libx264" : "libx265";
       args.push("-c:v", encoderName, "-preset", preset);
+      args.push("-threads", process.env.PRODUCER_FFMPEG_THREADS || "4");
       if (bitrate) args.push("-b:v", bitrate);
       else args.push("-crf", String(quality));
       if (codec === "h264") {
